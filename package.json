{
  "name": "smart-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Smart Task Manager portfolio project built with React, Node.js, Express, and MongoDB.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "start": "npm run start -w backend",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w frontend",
    "seed": "npm run seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "nodemailer": "^9.0.1"
  }
}
